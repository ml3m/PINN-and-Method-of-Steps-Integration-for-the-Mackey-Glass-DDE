# Mackey–Glass comparison: one-shot install
# Python 3.10+ recommended.
#
# torch is installed from PyTorch's CPU wheel index (smaller, runs on Linux/macOS/Windows
# without NVIDIA drivers). Training is slower than on GPU but reproduction works.
#
# For NVIDIA CUDA or AMD ROCm, reinstall torch afterward — see README.rst and
# requirements-rocm.txt.

--extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.0,<3
numpy>=1.24,<3
scipy>=1.11
matplotlib>=3.7
PyYAML>=6.0
